# Domain: persistence

Client-side and server-side storage strategies for AI-powered applications: chat history, conversation snapshots, project state, and metadata. Features here cover IndexedDB schemas, snapshot/versioning patterns, and how to replay or branch from saved states.

## Repos studied
- bolt.diy — IndexedDB chat history with project snapshots

## Related domains
- [[ai-integration]] — context optimization produces summaries stored as special messages
- [[runtime]] — snapshots capture the runtime's file state
